name: weather_project

version: "1.0"

config-version: 2

profile: weather_project

model-paths:
  - models

models:
  weather_project:
    staging:
      materialized: view

    marts:
      materialized: table
    analytics:
      schema: analytics
      materialized: table